# cd / -                    navigate to route

# cd ../ -                  Up one file

# ls list files in directory
# ls -a ALL FILES INCLUDING HIDDEN .fullstophiddenfiles
# ls -r REVERSE order list
# ls - R RECURSIVE list
# ls -i gives inode
#ls - l file list long format

# cd / -                    navigate to root

# cd /directory -           navigate to directory which is in the directory you are in

# cd home -                 navigate home

# cd -                      navigate to ~$

# cd ~ -                    navigate to ~$

# ls /home/local/FDMGROUP/  take a look in a directory without mocing

# mkdir -                   make a folder
# mkdir - p dir1/dir2       make parent(dir1) and child(dir2)

# touch -                   make a file

# cp -                      Copy SOURCE to DEST, or multiple SOURCE(s) to DIRECTORY
# example cp scenery.jpg /home/username/Pictures
#  create a copy of scenery.jpg (from your current directory) into the Pictures directory

# mv example: mv file.txt /home/username/Documents
# To rename files, the Linux command is mv oldname.ext newname.ext
# mv -                      Rename SOURCE to DEST, or move SOURCE(s) to DIRECTORY
# example: mv file.txt /home/username/Documents
# To rename files, the Linux command is mv oldname.ext newname.ext

# rm - remove files or directories
# example rmdir dir1 will delete empty directory
# example rmdir -r dir1 will remove directory and contents (recursive flag

#### GLOBBING ####

# * Match Zero or more character
# ? Match One character
# [] Match anything in [] for that character position e.g ls /dev/tty[0-9]
# The - is a rangae seperator and will match that range

#chmod amends read write execute permissions

