# grep - searches for PATTERN in each FILE.  A FILE of “-” stands for standard input
# qgrep [OPTIONS] PATTERN [FILE...]
# grep -E or egrep - use on extended regular expfessions
# grep -F fgrep - search for the string ignoring special meaning
# grep -i - ignore case
# grep -n - gives line number
# grep -v - match every thats not
# grep -w - match the whole word(still brings whole line)
# grep -c - count matching lines